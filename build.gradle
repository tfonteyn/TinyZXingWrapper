plugins {
    id("com.android.application") version "8.1.1" apply false
    id("com.android.library") version "8.1.1" apply false
    id("org.jetbrains.kotlin.android") version "1.9.10" apply false
    // https://github.com/mannodermaus/android-junit5
    // Don't forget to update ext.mannodermaus_version
    // id("de.mannodermaus.android-junit5") version "1.9.3.0" apply false
}

tasks.register("clean", Delete) {
    delete(rootProject.layout.buildDirectory)
}

ext {
    // build version for the library
    tiny_zxing_wrapper_version = "1.0.0"

    target_sdk_version = 34
    min_sdk_version = 26

    // https://github.com/zxing/zxing
    zxing_core_version = "3.5.2"

    // https://github.com/google/desugar_jdk_libs
    desugar_version = "2.0.3"

    androidx_activity_version = "1.7.2"
    androidx_fragment_version = "1.6.1"
    androidx_lifecycle_version = "2.6.1"

    androidx_camerax_version = "1.2.3"

    // https://github.com/material-components/material-components-android/releases
    google_material_version = "1.9.0"
}